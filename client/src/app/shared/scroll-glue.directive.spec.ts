import { ScrollGlueDirective } from './scroll-glue.directive';

describe('ScrollGlueDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollGlueDirective();
    expect(directive).toBeTruthy();
  });
});
